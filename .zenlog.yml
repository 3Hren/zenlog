severity: 0
pipelines:
  - name: main
    sources:
      - type: tcp
        endpoint: ["::", 50031]
    outputs:
      - type: file
        path: /var/tmp/zenlog-{severity}/zenlog.log
        pattern: "{timestamp}: {message}"
